# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy==2.0.36
asyncpg==0.30.0              # PostgreSQL async driver
alembic==1.14.0              # DB migrations
psycopg2-binary==2.9.11
greenlet>=3.0.0              # required by SQLAlchemy async

# ── Validation & Settings ──────────────────────────────────────────────────────
pydantic==2.9.0
pydantic-settings==2.6.0     # .env config management

# ── File Handling ──────────────────────────────────────────────────────────────
python-multipart==0.0.12     # file upload
aiofiles==24.1.0             # async file I/O
ffmpeg-python==0.2.0         # audio preprocessing via FFmpeg
soundfile==0.13.1

# ── AI / Speech Recognition ────────────────────────────────────────────────────
faster-whisper==1.1.1        # CTranslate2-based Whisper, 4x faster than openai-whisper
# Apple Silicon: device="cpu", compute_type="int8"（MPS 暂不被 CTranslate2 支持）
mlx-whisper==0.4.3

stable-ts==2.19.1

# silero-vad 依赖 torch，Apple Silicon 装 CPU 版即可（速度足够）
# 安装顺序：先装 torch，再装 silero-vad
# pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu
# pip install silero-vad==5.1.2
torch==2.3.1                 # silero-vad 依赖，CPU 版
torchaudio==2.3.1
silero-vad==5.1.2            # 静音检测，比 faster-whisper 内置 vad_filter 分段更精确

# ── Async Task Queue ───────────────────────────────────────────────────────────
celery==5.4.0                # background task processing
redis==5.2.0                 # celery broker + result backend

# ── HTTP Client (for LLM API calls) ───────────────────────────────────────────
httpx==0.27.0

# ── Dev / Testing ──────────────────────────────────────────────────────────────
pytest==8.3.0
pytest-asyncio==0.24.0
